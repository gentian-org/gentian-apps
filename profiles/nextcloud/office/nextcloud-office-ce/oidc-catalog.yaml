# OIDC pack for the public Nextcloud Office+ profile.
apiVersion: gentianos.io/v1alpha1
kind: OIDCPackCatalog
metadata:
  name: nextcloud-officeplus-oidc
  labels:
    gentianos.io/oidc-catalog: nextcloud-officeplus
    gentianos.io/profile-name: nextcloud-office-ce
spec:
  extraManagedByAttributeGroups:
    - FileshareOfficePlusAdmin
  mapperTemplates:
    gentian_useruuid:
      protocolMapper: oidc-usermodel-attribute-mapper
      config:
        user.attribute: entryUUID
        claim.name: gentian_useruuid
        jsonType.label: String
        id.token.claim: "true"
        access.token.claim: "true"
        userinfo.token.claim: "true"
    gentian_username:
      protocolMapper: oidc-usermodel-attribute-mapper
      config:
        user.attribute: uid
        claim.name: gentian_username
        jsonType.label: String
        id.token.claim: "true"
        access.token.claim: "true"
        userinfo.token.claim: "true"
    email:
      protocolMapper: oidc-usermodel-property-mapper
      config:
        user.attribute: email
        claim.name: email
        jsonType.label: String
        id.token.claim: "true"
        access.token.claim: "true"
        userinfo.token.claim: "true"
        introspection.token.claim: "true"
  packs:
    gentian-nextcloud-officeplus:
      scopeName: gentian-nextcloud-officeplus-scope
      scopeDescription: "Scope for Gentian Nextcloud Office+ app instances."
      clientRole: gentian-nextcloud-officeplus-access-control
      entitlementGroup: managed-by-attribute-FileshareOfficePlus
      publicClient: false
      fullScopeAllowed: false
      defaultScopes:
        - profile
        - email
        - roles
        - web-origins
        - acr
      mappers:
        - gentian_useruuid
        - gentian_username
        - email
