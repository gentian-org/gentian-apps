# OIDC pack for the public Nextcloud Suite profile.
apiVersion: gentianos.io/v1alpha1
kind: OIDCPackCatalog
metadata:
  name: nextcloud-suite-oidc
  labels:
    gentianos.io/oidc-catalog: nextcloud-suite
    gentianos.io/profile-name: nextcloud-suite
spec:
  extraManagedByAttributeGroups:
    - FileshareSuiteAdmin
  mapperTemplates:
    gentian_useruuid:
      protocolMapper: oidc-usermodel-attribute-mapper
      config:
        user.attribute: entryUUID
        claim.name: gentian_useruuid
        jsonType.label: String
        id.token.claim: "true"
        access.token.claim: "true"
        userinfo.token.claim: "true"
    gentian_username:
      protocolMapper: oidc-usermodel-attribute-mapper
      config:
        user.attribute: uid
        claim.name: gentian_username
        jsonType.label: String
        id.token.claim: "true"
        access.token.claim: "true"
        userinfo.token.claim: "true"
    email:
      protocolMapper: oidc-usermodel-attribute-mapper
      config:
        user.attribute: email
        claim.name: email
        jsonType.label: String
        id.token.claim: "true"
        access.token.claim: "true"
        userinfo.token.claim: "true"
        introspection.token.claim: "true"
  packs:
    gentian-nextcloud-suite:
      scopeName: gentian-nextcloud-suite-scope
      scopeDescription: "Scope for Gentian Nextcloud Suite app instances."
      clientRole: gentian-nextcloud-suite-access-control
      entitlementGroup: managed-by-attribute-FileshareSuite
      publicClient: false
      fullScopeAllowed: false
      defaultScopes:
        - profile
        - email
        - roles
        - web-origins
        - acr
      mappers:
        - gentian_useruuid
        - gentian_username
        - email
