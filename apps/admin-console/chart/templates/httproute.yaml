{{- if .Values.gateway.enabled }}
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "admin-console.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "admin-console.name" . }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.parentGatewayName }}
      namespace: {{ .Release.Namespace }}
  hostnames:
    - {{ printf "%s.%s" .Values.gateway.subDomain .Values.tenantDomain | quote }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api
      backendRefs:
        - name: {{ include "admin-console.fullname" . }}-api
          port: 8000
    - matches:
        - path:
            type: Exact
            value: /healthz
      backendRefs:
        - name: {{ include "admin-console.fullname" . }}-api
          port: 8000
    - matches:
        - path:
            type: Exact
            value: /readyz
      backendRefs:
        - name: {{ include "admin-console.fullname" . }}-api
          port: 8000
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: {{ include "admin-console.fullname" . }}-web
          port: {{ .Values.web.port }}
{{- end }}
